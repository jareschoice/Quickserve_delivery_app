// Stub for web - Firebase not needed for testing
import 'package:flutter/foundation.dart';

Future<void> initializeFirebase() async {
  debugPrint('Firebase skipped on web (no config needed for testing)');
}
